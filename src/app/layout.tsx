import { type Metadata } from 'next'

import { Providers } from '@/app/providers'
import { Layout } from '@/components/Layout'

import '@/styles/tailwind.css'

export const metadata: Metadata = {
  title: {
    template: '%s - Frank Baird',
    default:
      'Frank Baird - Senior Software Engineer , founder, and amateur astronaut',
  },
  description:
    'I’m Frank, a senior software engineer and entrepreneur based in Cincinnati, OH. I’m the co-founder and CEO of Baird Information Technology Solutions, where we design, develop, and deliver technology solutions that empower businesses to thrive, grow and take their business to the next level.',
  alternates: {
    types: {
      'application/rss+xml': `${process.env.NEXT_PUBLIC_SITE_URL}/feed.xml`,
    },
  },
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en" className="h-full antialiased" suppressHydrationWarning>
      <body className="flex h-full bg-zinc-50 dark:bg-black">
        <Providers>
          <div className="flex w-full">
            <Layout>{children}</Layout>
          </div>
        </Providers>
      </body>
    </html>
  )
}
